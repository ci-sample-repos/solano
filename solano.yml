ruby_version: 'ruby-2.3.0'
bundler_version: 1.10.6

plan:
- first
- second
- third

profiles:
  first:
    environment:
      MY_PROFILE: first
    test_pattern:
      - spec/agent_spec.rb

  second:
    environment:
      MY_PROFILE: second
    test_pattern:
      - spec/agent2_spec.rb

  third:
    environment:
      MY_PROFILE: third
    test_pattern:
      - spec/solano_api_spec.rb

