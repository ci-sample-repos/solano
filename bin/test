#!/usr/bin/env bash
git --version
echo '========= Config ============'
cat /tmp/agent2-config*
echo '=========  End   ============'
echo 'Test'
echo 'Start'
# sudo apt-get update
# sudo apt-get upgrade -y
echo 'Finish'
